language: generic
sudo: required
services:
 - docker

before_install: 
 - docker build -t singhpankaj23/docker-react -f Dockerfile.dev .

script: 
 - docker run singhpankaj23/docker-react npm run test
